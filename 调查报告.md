# AI Relay 添加站点与导入失败调查报告

## 1. 调查范围

调查对象是重新编译后，Options 页面添加站点和导入配置失败的问题。

本次只分析当前源码、构建产物、Git 差异和已有构建备份，没有修改业务源码。

## 2. 现象

添加站点时可以看到：

```text
[AI Relay][SiteForm] 权限请求结果: granted=true
[AI Relay] 原始消息 ADD_SITE from chrome-extension://.../src/options/index.html
sender={..., tab:true, ...}
```

导入配置时可以看到：

```text
[AI Relay] 导入：请求权限 [四个 host pattern]
[AI Relay] 导入：权限结果 { granted: true }
[AI Relay] 导入：发送 IMPORT_CONFIG（第 1 次，超时 12s）
[AI Relay] 原始消息 IMPORT_CONFIG from chrome-extension://.../src/options/index.html
[AI Relay] 导入：发送 IMPORT_CONFIG（第 2 次，超时 12s）
```

## 3. 结论

主因是后台消息路由新增的发送方校验逻辑错误地把合法的扩展 Options 页面消息当成了 content script 消息。

权限申请已经成功，消息也已经到达 Service Worker；失败发生在消息路由层，`ADD_SITE` 和 `IMPORT_CONFIG` handler 根本没有被执行。

## 4. 证据链

### 4.1 权限不是主因

`src/options/components/SiteForm.vue` 在提交时申请 `${origin}/*` 权限，日志返回 `granted=true`。

导入流程在 `src/options/App.vue` 中一次申请全部站点权限，日志同样返回 `granted=true`。

后台 `ADD_SITE` handler 还会再次调用 `chrome.permissions.contains()` 做校验。如果权限失败，应该看到 handler 被调用后产生的错误，而不是只有“原始消息”日志。

### 4.2 消息已经到达后台

`src/background/router.ts` 在路由入口打印“原始消息”。日志中的来源是：

```text
chrome-extension://jlicpjifklfcggeammiomjoghgjdmcpo/src/options/index.html
```

这明确说明消息来自本扩展的 Options 页面，而不是 `https://...` 普通网页。

### 4.3 路由校验拦截了消息

当前 `src/background/router.ts` 第 7 行附近新增了：

```ts
if (sender.tab) {
  return false
}
```

同时，路由器在校验失败时返回 `FORBIDDEN`，不会调用：

```ts
handler(req.payload, req)
```

当前日志中没有出现以下成功路径日志：

```text
[AI Relay] 路由：调用 handler ADD_SITE
[AI Relay] 路由：调用 handler IMPORT_CONFIG
```

因此可以确定请求在 `isTrustedSender()` 阶段被拦截。

### 4.4 导入第二次请求是错误重试的放大结果

`src/options/App.vue` 的 `sendImportWithRetry()` 对所有异常执行一次重试：

```ts
catch (e) {
  if (attempt < 2) {
    return sendImportWithRetry(cfg, attempt + 1)
  }
}
```

这包括 `FORBIDDEN`。因此第二次 `IMPORT_CONFIG` 不是权限再次申请成功，也不是新的故障，而是同一个被路由拒绝的问题被重复发送。

## 5. 排除项

### 5.1 不是 host 权限配置错误

当前 manifest 使用：

```ts
host_permissions: []
optional_host_permissions: ['<all_urls>']
```

这是允许运行时按具体站点申请权限的配置。日志已经证明具体权限申请结果为 `true`。

### 5.2 不是 URL pattern 格式错误

日志中的权限格式是：

```text
https://api.hubway.cc/*
```

该格式符合 Chrome host permission pattern 规则。

### 5.3 不是构建文件名变化

`index.ts-BQbzUe34.js`、`index.html-Dmrkq5GM.js` 等文件名是 Vite 根据内容生成的 hash 文件名，重新编译后变化是正常现象。当前构建产物确实包含新增的错误路由校验，但文件名本身不是故障原因。

### 5.4 当前证据不足以指向 IndexedDB

导入 handler 中确实增加了 IndexedDB 看门狗和批量导入逻辑，但当前请求尚未进入 `IMPORT_CONFIG` handler，暂不能把数据库迁移或写入逻辑认定为本次主因。

## 6. 影响范围

受影响的不只是添加和导入：所有从 Options 或 Side Panel 通过 `chrome.runtime.sendMessage()` 发送、且被该校验识别为带 `sender.tab` 的请求，都可能被拒绝，例如站点更新、删除、采集和数据查询。

## 7. 根因归属

这是新增的消息来源安全校验与实际 Chrome `MessageSender` 行为之间的假设不一致：代码假定“扩展页面一定没有 `sender.tab`”，但实际日志显示当前扩展 Options 页面消息带有 `sender.tab=true`。

校验顺序又把 `sender.tab` 放在扩展 URL/扩展 ID识别之前，导致合法扩展页面先被拒绝。

