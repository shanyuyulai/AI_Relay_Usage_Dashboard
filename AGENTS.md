# 项目记忆与协作约定

## 基本工作约定

- 所有需求和问题先充分理解现有代码与文档。
- 只有用户明确授权后，才修改代码或文件。

## 方案文档固定命名规则

- 用户要求“生成方案”时，必须生成可落地的 Markdown 方案文档（如果你的方案包含丰富的流程图架构图等md不满足的情况，可以视情况改为word等其他格式），统一放在项目根目录的AI_documentation文件夹下(第一次文档落地时如果没有此文件夹，请先新建，然后再放进去)，修改bug等的非设计、非需求的这种中间产物的文档放置到AI_documentation/modify文件夹下。
- 文件名格式使用：`序号_日期_方案名称.md`（序号从001开始），日期格式为 `YYYY_MMDD`。
- 例如：`018_2026_0814_自动适配器识别与侧边栏六项指标调整方案.md`。
- 序号使用全局递增编号，不因主题变化重新计数。
- 如果当天同一问题有新的方案，使用下一个全局序号，不添加子序号：
  `019_2026_0814_xxxx方案.md`。
- 如果以后针对同一问题有新的方案，在下一个全局序号后增加三位子序号，并更新日期：
  `019_001_2026_0819_xxxx方案.md`。
- 同一问题后续再次跨日期更新时，子序号依次递增为 `002`、`003` 等；如果当天继续产生新版本，仍按当天规则使用下一个全局序号且不添加子序号。
- 已有历史文档不强制批量改名；只有用户明确要求时才重命名历史文件。
