# AI Relay 消息路由故障修改方案

## 1. 修改目标

恢复 Options 和 Side Panel 到 Service Worker 的正常消息通信，同时保留对普通网页、content script 和其他扩展来源消息的拦截能力。

## 2. 最小修改方案

### 2.1 调整发送方判断顺序

修改 `src/background/router.ts` 的 `isTrustedSender()`：

1. 先根据 `sender.url` 和 `sender.id` 判断是否为本扩展自己的 `chrome-extension://` 页面。
2. 确认属于本扩展后直接放行，不再因为 `sender.tab` 存在而拒绝。
3. 对其他扩展 URL 拒绝。
4. 对普通网页来源且带 `sender.tab` 的消息拒绝。
5. 保留 localhost 开发模式兼容，但应限制在明确的开发环境，不要对任意来源放宽。

建议判断原则如下，具体代码以当前项目的运行模式为准：

```ts
const selfId = chrome.runtime.id
const url = sender.url ?? ''
const ownExtensionUrl = selfId
  ? url.startsWith(`chrome-extension://${selfId}/`)
  : false

if (ownExtensionUrl) return true
if (url.startsWith('chrome-extension://')) return false
if (sender.tab) return false
```

重点是不能再把 `sender.tab` 作为第一优先级的绝对拒绝条件。

### 2.2 限制导入重试条件

修改 `src/options/App.vue` 的 `sendImportWithRetry()`，只对通信类暂时性错误重试，例如：

- `TIMEOUT`
- `EMPTY`
- `RUNTIME`

以下错误不应重试：

- `FORBIDDEN`
- `UNKNOWN_TYPE`
- `BAD_REQUEST`
- handler 返回的业务错误

这样可以避免权限错误、来源校验错误或参数错误被重复发送，日志也会更准确。

### 2.3 保持后台权限二次校验

不要删除 `ADD_SITE` 和 `IMPORT_CONFIG` handler 中的 `chrome.permissions.contains()` 校验。权限申请应由 UI 发起，后台仍然必须独立验证权限，避免仅信任 UI 传入结果。

## 3. 建议修改文件

### 必改

- `src/background/router.ts`
- `src/options/App.vue`

### 不需要为了本问题修改

- `manifest.config.ts`
- `src/shared/permissions.ts`
- `src/options/components/SiteForm.vue`
- `src/background/handlers.ts` 的站点权限校验逻辑

## 4. 验证方案

### 4.1 编译验证

执行：

```text
pnpm run typecheck
pnpm run build
```

确认没有 TypeScript、Vue 模板和构建错误。

### 4.2 添加站点验证

使用一个未授权域名，例如 `https://api.hubway.cc`：

1. 点击添加站点。
2. 确认权限弹窗返回 `granted=true`。
3. 确认后台出现：

```text
[AI Relay] 原始消息 ADD_SITE ...
[AI Relay] 路由：调用 handler ADD_SITE
[AI Relay] 路由：handler 成功 ADD_SITE
```

4. 确认站点出现在列表中。

### 4.3 导入验证

使用包含多个站点的配置文件：

1. 确认一次权限申请返回 `granted=true`。
2. 确认只发送一次 `IMPORT_CONFIG`；除非发生真正的通信超时，不应出现第二次。
3. 确认后台出现 `IMPORT_CONFIG 入口`、逐站权限检查和导入结束日志。
4. 确认新增、更新和跳过数量与 UI 提示一致。

### 4.4 安全回归验证

验证以下消息仍然被拒绝：

- 普通网页来源的消息；
- content script 来源的消息；
- 其他扩展来源的消息。

验证以下消息可以正常通过：

- Options 页面消息；
- Side Panel 页面消息；
- Service Worker 自发广播消息。

## 5. 发布与加载注意事项

修改后必须重新执行构建，并在 `chrome://extensions` 中点击“重新加载”。同时重新打开 Options 页面，确保页面脚本和 Service Worker 来自同一份 `dist`，避免出现新旧构建混用。

## 6. 回滚方案

如果修复后出现新的来源校验问题，可以先回滚 `src/background/router.ts` 的新增 `isTrustedSender()` 逻辑，恢复原有消息路由，再单独以扩展 ID 和来源 URL为基础补充安全校验。不要通过删除权限申请逻辑或改成静态 `<all_urls>` 权限来规避问题。

## 7. 本轮执行边界

本轮仅生成调查报告和修改方案，没有执行源码修改、构建或提交 Git。后续需要明确授权后再修改代码。
