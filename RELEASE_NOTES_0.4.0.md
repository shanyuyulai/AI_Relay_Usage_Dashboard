# AI 中转站用量看板 0.4.0

## 新功能与改进

- 增加插件总开关及每站点Switch，站点Switch紧跟名称后的适配器标记，设置页标题旁直接控制插件；侧边栏和极简面板保持简洁，不新增开关。
- 实现每站最多10条余额警报，支持下降穿越临界值、重复抑制、回升后重新触发、合并系统通知及失败重试。首次有效采集仅建立比较基准。
- 站点编辑采用响应式双列布局，保存/取消操作保持可见；长说明可展开查看。
- 自定义采集、诊断日志展开区新增明显的“× 关闭”按钮；本页会话保留未保存自定义草稿，诊断异步加载避免串站。
- 统一滚动条样式，适配浅色/深色主题并保留原生滚动。
- 设置列表、侧栏卡片及站点详情默认尝试网站图标，失败时回退彩色首字；匿名、同源、有限并发与缓存，不新增权限或第三方图标服务。
- **极简面板不显示站点图标**，保留状态圆点、站点名称和余额。
- 包含此前工作区的真实人民币余额展示、统计周期与采集兼容性改进。

## 安装与升级

1. 下载本Release的`AI_Relay_Usage_Dashboard_v0.4.0.zip`，不是GitHub自动生成的Source code压缩包。
2. 解压后应直接看到`manifest.json`；Chrome/Edge扩展管理页开启开发者模式，加载该目录。
3. 升级前请导出全量备份。建议将新文件替换到原加载目录，点击重新加载，不要直接卸载旧扩展。
4. 重新打开设置页、侧边栏和极简面板。网站图标获取失败会正常显示首字；极简面板不会请求图标。
5. `SHA256SUMS.txt`提供安装包校验和。该ZIP为开发者模式侧载包，并非商店签名CRX。

## 兼容与已知边界

- 数据库升级至v6，保留既有站点和历史数据；运行时警报记录不纳入可移植备份。
- 系统通知是否弹出、停留时间受浏览器、操作系统和免打扰设置影响。
- 关闭插件会停止新的业务任务并等待/清理已开始的工作，已发送的网络请求无法撤回；不宣称所有活动请求都可主动取消。
- 网站图标仅支持已授权的公开同源资源；跨域CDN、登录保护、重定向或离线可能回退首字，不影响采集。
- 自动化测试不替代用户Chrome/Edge的真实站点视觉与系统通知验收。

## 验证

83项自动化测试全部通过，TypeScript/Vue类型检查和Vite生产构建通过；安装包已检查manifest版本、资源文件和SHA-256校验和。真实浏览器视觉与系统通知效果仍需使用环境验收。
